const UIToolModes = {
    SELECT: 'select',
    DRAG: 'drag',
    PET: 'pet'
};

class UIComponent {
    constructor(parent) {
        this.parent = parent;
    }

    init() {}

    update() {}

    dispose() {}
}
